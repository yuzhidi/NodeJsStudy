// var assert = require('assert');
// assert.equal(1, true, 'Truthy');
// assert.notEqual(1, true, 'Truthy');
// assert.ok('This is a string', 'Strings that are not empty are truthy');

console.log(Buffer.from("0"));
console.log(Buffer.from("*ALL"));
console.log(Buffer.from(["0", "1"]));
console.log(Buffer.from(["01", "1"]));
console.log(Buffer.from(["*ALL", "1"]));
